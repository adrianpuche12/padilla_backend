# ===========================================
# Production Profile - Docker Internal Network
# ===========================================

# SSH Tunnel disabled in production
ssh.tunnel.enabled=false

# PostgreSQL via Docker internal network
spring.datasource.url=jdbc:postgresql://postgres-padilla:5432/padilla-db
spring.datasource.username=padilla
spring.datasource.password=gw4^PX9CbXJwG!
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# Logging
logging.level.org.springframework.security=WARN
spring.jpa.show-sql=false
